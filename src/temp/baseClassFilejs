export default class {baseClassName} {
  request(config) {
    // TODO 需自行实现请求逻辑
    return Promise.resolve({})
  }

  downloadFile(config) {
    // TODO 需自行实现下载逻辑
    return Promise.resolve({})
  }
}
